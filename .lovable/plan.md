
## Make AI Typing Indicator Purple

Update the `TypingIndicator` component in `ChatInterface.tsx` to match the AI purple theme.

### Changes in `src/components/module/ChatInterface.tsx`

**TypingIndicator** (lines 106-120):
- Change container background from `bg-muted` to `bg-aipurple/10`
- Change dot colors from `bg-muted-foreground` to `bg-aipurple`

This makes the typing animation visually consistent with the AI message bubbles and overall purple theme.
