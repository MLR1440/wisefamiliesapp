

## Make "Wiser" Gold in Hero Heading

Wrap the word "Wiser" in a `<span className="text-gold-400">` in `src/components/landing/Hero.tsx` to give it the brand's golden yellow color, creating a nice contrast with the purple "AI" text.

### File Changed
- `src/components/landing/Hero.tsx` — wrap "Wiser" in gold-colored span

